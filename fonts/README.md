Place your downloaded font files here (e.g. CustomFont.woff and CustomFont.woff2).

Ensure the names match the `src` URLs in `style.css`'s `@font-face` rule, or update that rule accordingly.

You may add additional `@font-face` rules in `style.css` if you use multiple font weights or styles.